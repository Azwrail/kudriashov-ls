.header {
    display: flex;
    justify-content: space-around;
    align-items: baseline;
}
.logo {
    font-style: normal;
    font-weight: 800;
    font-size: 36px;
    line-height: 49px;
    text-decoration: none;
    &:hover {
        color: #FF9A00;
        text-decoration: underline;
    }
}